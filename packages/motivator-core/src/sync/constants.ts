/**
 * Параметры отложенной синхронизации ciphertext vault с сервером (см. VaultProvider в web).
 */

/** Задержка после последней локальной правки перед записью в хранилище (мс). */
export const VAULT_REMOTE_SAVE_DEBOUNCE_MS = 450

/**
 * Порог предупреждения (размер JSON до шифрования, байты UTF-8).
 * Не ограничивает сохранение — только подсказка для профилирования / будущих лимитов.
 */
export const VAULT_JSON_WARN_BYTES = 1_500_000
