---
description: Секреты Amvera и VITE_* — только в LK и build override; таблица имён в docs/amvera-secrets.md, не в git
globs: ["amvera.yaml", "docs/amvera-secrets.md", "scripts/build-amvera.mjs", "deploy/**", "web/.env.example"]
alwaysApply: false
---

# Секреты и env Amvera (адаптер Cursor)

Тонкий адаптер. Источник истины — `.cursor/skills/amvera-secrets-and-env/SKILL.md`.

Коротко: значения не в git; VITE_* в build.additionalCommands; runtime secrets в planner-api; новый секрет → строка в docs/amvera-secrets.md.
