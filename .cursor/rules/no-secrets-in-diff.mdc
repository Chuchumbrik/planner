---
description: Перед коммитом — патч не должен содержать секретов (токены, приватные ключи, строки подключения с паролем)
globs: ["**/*"]
alwaysApply: true
---

# Нет секретов в диффе (адаптер Cursor)

Тонкий адаптер. Источник истины — `.cursor/skills/no-secrets-in-diff/SKILL.md` (открыть skill `no-secrets-in-diff`).

Коротко: scan staged diff на секреты до попадания в git; проверка `scripts/check-gates/no-secrets-in-diff.mjs` (**warn**; **block** с `GATE_BLOCK=1`). Закрытие — `security-hygiene`, секреты только env / Amvera LK.
