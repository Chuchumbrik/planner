---
description: Новый логический код в web/src, packages/*/src или services/*/src должен сопровождаться изменением соответствующего теста в том же коммите
globs: ["web/src/**/*.ts", "web/src/**/*.tsx", "packages/*/src/**/*.ts", "packages/*/src/**/*.tsx", "services/*/src/**/*.ts", "services/*/src/**/*.tsx"]
alwaysApply: false
---

# Новый код несёт тесты (адаптер Cursor)

Тонкий адаптер. Источник истины — `.cursor/skills/tests-for-new-code/SKILL.md`.

Коротко: логика в `web/src`, `packages/*/src`, `services/*/src` — колокированный тест в том же коммите. Проверка: `scripts/check-gates/tests-for-new-code.mjs` (**block**).
